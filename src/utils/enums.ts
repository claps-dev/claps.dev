export enum DonationDistribution {
  PersperAlgorithm,
  Commits,
  ChangedLines,
  IdenticalAmount,
}

export enum RepositoryType {
  GITHUB = 'GitHub',
  BITBUCKET = 'BitBucket',
  GITLAB = 'GitLab',
  GIT = 'Git',
}

checks:
  - name: build-size-watcher
    options:
      files:
        - path: 'dist/**/*.*'
        - path: 'node_modules/.nuxt/dist/**/*.*'
  - name: type-coverage-watcher
    options:
      ignoreCatch: true
      ignoreFiles:
        - '**/*.d.ts'
      strict: true
